# LLM — Codex CLI is the default and reuses `codex login` authentication.
LLM_PROVIDER=codex
LLM_MODEL=gpt-5.6-luna
# Optional compatibility backend: set LLM_PROVIDER=claude and choose a Claude model.

# Transcription — provider chosen by the Day 1 bake-off, see BAKEOFF.md
DEEPGRAM_API_KEY=
ASSEMBLYAI_API_KEY=

# Site (Day 5)
DATABASE_URL=
AUTH_SECRET=
